TransparentClock is simple desktop clock for composite window manager. It was
influenced BeClock from BeKWinFX. It requires:

* QtCore
* QtGui
* QtWidgets
* Enabled composite window manager

```
qmake
make
./TransparentClock
```
